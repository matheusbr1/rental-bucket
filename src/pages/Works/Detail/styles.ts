import styled from 'styled-components'

export const Container = styled.div`
  .floatting-buttons {
    display: flex;
    position: absolute;
    bottom: 50px;
    right: 50px;
  }

  .floatting-buttons.left {
    left: 50px;
  }
`